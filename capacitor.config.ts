import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cambiosgr.app',
  appName: 'CambiosGr',
  webDir: 'dist/angula17/browser'
};

export default config;
